변수

1Bit
- 0 과 1로된 단위
11111111 -> 2^7 = 128

1Byte
 - 8 bit 로 구성 0000 0000 이것은 16진수로 0X00 으로 표현
 2^8 = 256 -> -128 ~ 127
 
- boolean의 경우
  1bit로 충분하지만, 데이터를 다루는 최소단위가 1Byte이기 때문에 사용
 
- byte, short 가 더 적은  Bit를 가지고 있어 메모리를 조금 더 절약할수 있지만  int를 사용하자
  *JVM 의 피연산자 스택이 피연산자를 4 Byte 단위로 저장 하기 때문에
  int 보다 작은 자료형의 값을 계산시 int 형으로 형변환 되서 연산이 수행됨

#실수형
이진수로 실수를 표현하기 위해 어떤 방법을 써야 할까? 실수가 정수라면 다행히지만 실수는 정수 뿐만 아니라 소수도 포함한다. 
이진수로 소수를 표현하기 위해 어떤 방법을 고안 해 냈을까?
사람들은 이진수로 소수를 표현하기 위해 다양한 방법을 고안 했지만 현재는 IEEE에서 지정한 부동 소수점 표현방식(Floating Point Representation)이 보편적으로 사용된다. 
간단히 말하자면 어떤 수를 과학에서 유효숫자를 표현하듯 (예를들어 130 = 1.3 * 10^2) 이진수 또한 사용 가능한 비트를 지수부와 가수부로 나누어 지수부에서는 지수(2^n)를 표현하고 가수부는 말 그대로 x.xxxx부분을 표현하는 것이다. 
부동 소수점은 CPU의 FPU(Floating Point Unit)이란 곳에서 해석되고 계산된다.
0.31415 => 0.31415 X 10^1

float
S(1) + E(8) + M(23)
부호, 지수, 가수로 정해진 비트수
2^32 -> 32Bit -> 8Byte

double
dobule이 float에 비해 가수부의 bit가 훨씬 크기 때문에 정밀한 소수를 표헌할 수 있다

#지역변수
-사용전 반드시 초기화

#전역변수
-클래스변수, 인스턴스변수  뒤에서 계속




#참고
https://mirwebma.tistory.com/77
https://leejiheg.tistory.com/entry/Java-%EA%B8%B0%EB%B3%B8-%EB%B3%80%EC%88%98
https://imasoftwareengineer.tistory.com/50